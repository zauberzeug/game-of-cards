## 2026-06-22T00:00:00Z — Closure

- **What changed**: `goc/templates/hooks/deck_session_start.py` `_card_waiting_on` — drop parser-coerced bool/int tokens (`_TRUE_SET ∪ _FALSE_SET` / `_INT_RE`) to None, mirroring the engine's `isinstance(v, str)` guard; same narrowing ported to `openclaw-plugin/index.ts` via a new `waitingOnScalar` reader. `waiting_until` readers left unchanged (no engine isinstance guard there). Plugin mirrors regenerated by `sync-plugin-assets`.
- **Verification**: `reproduce.py` exits 0 — `false`/`true`/`yes`/`no`/`42` all resolve `engine.waiting_impedes=False` and `hook._is_impeded=False`. New regression tests: 2 Python (`test_session_start_hook`) + 5 TS cells (`test_openclaw_session_start_hook`).
- **Audit**: PASS — no rubric configured; mechanical fix mirroring the engine's documented `isinstance(v, str)` contract.
- **Project impact**: n/a
- **Tests**: 509 passed / 0 failed.
- **Bundled with**: n/a

## Closure verification (2026-06-22T08:46:12Z)

### Layer-3 (GoC DoD)

- [x] advanced-by-closed — no advanced_by edges
- [x] dod-100-percent — 6/6 ticked
- [x] log-md-closure-entry — '## 2026-06-22 — Closure' present
