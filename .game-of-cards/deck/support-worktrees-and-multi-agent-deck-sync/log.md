## 2026-05-09: renamed from support-multi-branch-and-multi-user-deck-workflows

## 2026-05-09: decision recorded

Lower epic gate to none. Both open children are now agent-decidable: claim-protocol policy decisions recorded (free-form worker.who, last-writer-wins+retry, opt-in integration check); evaluate-deck decision recorded (same-repo only ships supported, alternatives documented as possible-but-unsupported). The epic's remaining DoD is verification — agents can verify children close, confirm README/personas already cover the workflows, and run `uv run goc validate`. — The session gate existed only to integrate the children's outputs. With those policy choices recorded, integration is mechanical, not judgmental — no further human input is required to pull this card.. Gate session → none.

## 2026-05-09: closed

Verification roll-up. All three children are now closed: `spike-worktree-auto-resolves-deck-from-main-repo` (worktree path resolution shipped), `design-claim-protocol-with-branch-and-author-metadata` (`workflow.claim_push` + `workflow.closure_on_integration` in `goc/engine.py`), and `evaluate-deck-as-separate-repo-or-submodule` (same-repo decision + `DECK_LOCATION.md` trade-off write-up). README's "Who this is for" preamble names the three primary personas and cross-links `PERSONAS.md`, which carries an explicit "Workflow shape" subsection per persona (mainline-only for vibe-coders, mainline-or-branch for solo devs, mainline-primary with claim protocol for multi-agent coordinators, external deck for classical-dev / OSS, deferred for non-code agent runtimes). `uv run goc validate` passes. No epic-level policy questions remain — the three orthogonal failure modes (worktrees, multi-human/multi-AI, OSS commit hygiene) each have a documented and verified workflow story.
