## 2026-08-15T05:02:00Z — Closure

- **What changed**: `AGENTS.md:148-156` — the `## Code architecture` bullet
  for `goc/cli.py` now describes the real wiring (SIGPIPE restore, `argv[0]`
  intercept for `install`/`upgrade` into standalone parsers, delegation to
  `engine.cli()` which builds the parser and owns `--version`) instead of
  three clauses the code never performed. `goc/cli.py:11` drops the unused
  `_build_parser` import and its module docstring is re-derived the same way;
  the three plugin mirrors were regenerated by
  `scripts/sync_plugin_assets.py`. `tests/test_guidance_accuracy.py` pins all
  three claims to the tree via a new behavioural test plus three doc tests
  and a shared `_agents_cli_bullet()` helper.
- **Verification**: `reproduce.py` 1 → 0 (3 false claims → 0).
  `tests.test_guidance_accuracy` 30 tests OK (was 26). Each new assertion was
  checked against the stale bullet text: the 3 negative regexes match it and
  the 2 positive containment checks fail on it, so the guard catches the
  offender rather than only reporting a clean tree.
  `scripts/sync_plugin_assets.py --check` byte-for-byte clean.
  `uv run goc validate` exits 0.
- **Audit**: no rubric configured (`.game-of-cards/hooks/finish-card.md` is
  an empty stub); mechanical doc + dead-import fix, no principle bound.
- **Project impact**: n/a
- **Tests**: 975 run — 974 passed / 1 failed. The single failure is
  `test_canonical_tag_rows.test_live_cards_satisfy_every_state_row`, red on
  `main` before this card's first edit and tracked by
  `regression-suite-red-on-main-over-the-unverified-tag-row`; its offender is
  `openclaw-pattern-check-never-fires-on-plain-file-edits`, untouched here.
  The card's last DoD item was reworded mid-closure to state that contract
  honestly rather than claim a fully green suite.
- **Bundled with**: n/a

Filed and closed in the same session under `Skill(pull-card)`'s fix-through
rule: the ready queue was empty (all three `human_gate: none` open cards
carry active `waiting_on` overlays), so `Skill(audit-deck)` surfaced this,
and it cleared the fix-through bar — gate-free, single-site, and connecting
to the existing `tests/test_guidance_accuracy.py` root rather than opening an
eighth AGENTS.md-drift umbrella.

## Closure verification (2026-08-15T04:42:28Z)

### Layer-3 (GoC DoD)

- [x] advanced-by-closed — no advanced_by edges
- [x] dod-100-percent — 6/6 ticked
- [x] log-md-closure-entry — '## 2026-08-15 — Closure' present
