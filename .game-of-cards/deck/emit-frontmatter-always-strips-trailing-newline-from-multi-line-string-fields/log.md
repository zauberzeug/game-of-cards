## 2026-06-08T05:04:50Z — Closure

- **What changed**: `goc/engine.py:326-334` — `emit_frontmatter` now selects the multi-line-string block chomp indicator from the value (`|` when it ends in a newline, `|-` otherwise) instead of hard-coding `|-`; docstring updated to match. Plugin mirrors (`claude-plugin`, `codex-plugin`, `openclaw-plugin`) regenerated by `scripts/sync_plugin_assets.py`.
- **Verification**: `reproduce.py` exits 0 — authored `summary: |` round-trips with its trailing newline and `|` indicator intact; a value with no trailing newline still emits `|-`. Re-emit is idempotent for both states.
- **Audit**: PASS — no project rubric configured; mechanical fix (field-symmetric frontmatter serialization — restores emit↔parse round-trip fidelity, the same invariant as the closed emitter-round-trip card family).
- **Project impact**: n/a
- **Tests**: 407 passed / 0 failed (full `unittest discover`), incl. 3 new `BlockScalarIndicatorRoundTripTest` cases; `goc validate` clean.

## Closure verification (2026-06-08T05:05:11Z)

### Layer-3 (GoC DoD)

- [x] advanced-by-closed — no advanced_by edges
- [x] dod-100-percent — 5/5 ticked
- [x] log-md-closure-entry — '## 2026-06-08 — Closure' present
