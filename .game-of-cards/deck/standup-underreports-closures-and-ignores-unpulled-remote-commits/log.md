## 2026-05-27T18:21:06Z — Closure

- **What changed**: `goc/templates/skills/standup/SKILL.md` — Section 3 ("Closed since yesterday") now lists cards whose engine `closed_at` falls in the last 24h, read from `goc --json --status all`, replacing the git-blind `find … -newer .game-of-cards/deck -mmin -1440` mtime scan. Added a `git fetch` + behind-upstream sync warning as the first Context block, a Preflight note, and corrected the intro's self-description (it claimed to use `git log` but read mtime). Mirrors regenerated by `scripts/sync_plugin_assets.py` (.claude + .codex + claude-plugin + codex-plugin).
- **Verification**: new Section 3 command lists **62** closures in the last 24h (58 done + 3 disproved + 1 superseded) where the old `find` reported **0** ("Nothing closed"); deck dir and every log.md shared mtime `15:01:56`, which is what collapsed the strict `-newer` test. `reproduce.py` exits 0 post-fix (exit 1 pre-fix, with a deterministic fixture proving the `-newer` blindness). Sync-state command reports current-with-upstream and degrades silently when offline / no upstream.
- **Audit**: PASS — no rubric configured (`.game-of-cards/hooks/finish-card.md` empty, `layer_2_project_dod: []`); mechanical correctness fix — derive recency from the structured `closed_at` record instead of a filesystem mtime proxy.
- **Project impact**: n/a
- **Tests**: no pytest suite in repo (per AGENTS.md); `uv run goc validate` OK (no FAIL/ERROR; 2 pre-existing WARNs on unrelated cards), `python scripts/sync_plugin_assets.py --check` OK (byte-for-byte parity).
- **Bundled with**: none.

### Root-cause note for future readers

Both defects shared one root: standup inferred "recent activity" from local filesystem facts (file mtime; working-tree contents) rather than the records that carry the truth — the engine's `closed_at` field and git's relationship to the remote. Found when a live standup reported "Nothing closed in the last 24h" the morning after a sync landed 169 commits / 62 closures. The fix is not "use git instead of mtime" but "use the structured record"; the sync-check is the separate, quieter half — standup never verified the local tree was even current before trusting it.

## Closure verification (2026-05-27T18:21:26Z)

### Layer-3 (GoC DoD)

- [x] advanced-by-closed — no advanced_by edges
- [x] dod-100-percent — 6/6 ticked
- [x] log-md-closure-entry — '## 2026-05-27 — Closure' present
