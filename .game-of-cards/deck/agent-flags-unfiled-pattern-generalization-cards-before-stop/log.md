## 2026-05-05: decision recorded

A+B+A: lightweight prompt-only Stop hook, fires only after code-mutating tool calls, reminder-only (no stop-block); default-on with opt-out via .game-of-cards/config.yaml (hooks.pattern_generalization_check) — Cheapest experiment that addresses the real failure mode; FP cost is structurally low (extra system reminder, no forced action), so opt-in gating would suppress adoption. Promote axes only if FP/FN rate proves unacceptable in practice.. Gate decision → none.
